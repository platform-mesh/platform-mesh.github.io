# Components

Platform Mesh is delivered as a set of loosely-coupled functional components.
Each component can be deployed and operated independently, but they are designed to work together on top of
a [kcp](https://kcp.io)-based multi-tenant control plane.
