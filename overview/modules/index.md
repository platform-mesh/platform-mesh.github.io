# Modules

Platform Mesh is delivered as a set of loosely-coupled functional modules.
Each module can be deployed and operated independently, but they are designed to work together on top of
a [kcp](https://kcp.io)-based multi-tenant control plane.
