import{_ as d,C as c,o as t,c as s,a5 as o,b as i,w as a,a as n,E as l,a6 as h}from"./chunks/framework.CpHIsX1t.js";const w=JSON.parse('{"title":"virtual-workspaces","description":"","frontmatter":{},"headers":[],"relativePath":"reference/components/virtual-workspaces.md","filePath":"reference/components/virtual-workspaces.md"}'),p={name:"reference/components/virtual-workspaces.md"};function u(m,e,f,k,g,v){const r=c("Mermaid");return t(),s("div",null,[e[1]||(e[1]=o('<h1 id="virtual-workspaces" tabindex="-1">virtual-workspaces <a class="header-anchor" href="#virtual-workspaces" aria-label="Permalink to &quot;virtual-workspaces&quot;">​</a></h1><p>The Platform Mesh <code>virtual-workspaces</code> component is a standalone virtual workspace apiserver built on kcp&#39;s <code>pkg/virtual/framework</code>. It hosts two named virtual workspaces — <code>contentconfigurations</code> and <code>marketplace</code> — that serve curated, multi-workspace views of UI extension data and the service marketplace catalog.</p><p>This page documents the deployed Platform Mesh component. It is distinct from the kcp framework primitive described in <a href="./kcp/virtual-workspaces.html">kcp virtual workspaces</a>, which the component builds on.</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>This component is in alpha. CLI flags, the <code>MarketplaceEntry</code> schema, authentication, authorization, and deployment wiring may change on short notice, including breaking changes.</p></div><h2 id="purpose" tabindex="-1">Purpose <a class="header-anchor" href="#purpose" aria-label="Permalink to &quot;Purpose&quot;">​</a></h2><p>The server exposes two read endpoints:</p><ul><li>The <code>contentconfigurations</code> virtual workspace serves a merged view of <code>ContentConfiguration</code> resources, drawing from the system workspace and from any provider workspace that the consumer&#39;s account workspace binds. The Platform Mesh Portal uses this view to assemble navigation for the active workspace.</li><li>The <code>marketplace</code> virtual workspace serves a <code>MarketplaceEntry</code> view derived from <code>APIExport</code> state. The Marketplace UI uses this view to render the provider catalog.</li></ul><p>Mutations happen elsewhere. Installing a provider creates an <code>APIBinding</code> in the consumer workspace, not in the virtual workspace.</p><h2 id="runtime-role" tabindex="-1">Runtime role <a class="header-anchor" href="#runtime-role" aria-label="Permalink to &quot;Runtime role&quot;">​</a></h2><p>The binary runs the <code>start</code> subcommand and serves an HTTPS apiserver on the configured port (chart default <code>8443</code>). Clients reach it through the kcp front-proxy at <code>/services/&lt;vw-name&gt;/...</code>; the front-proxy mapping is owned by the kcp installation, not by the <code>virtual-workspaces</code> chart.</p><p>The server does not own any persistent state. At runtime it:</p><ul><li>Connects to the kcp root through the configured kubeconfig.</li><li>Reads <code>apis.kcp.io/v1alpha1.APIResourceSchema</code> for the <code>contentconfigurations</code> schema and <code>apis.kcp.io/v1alpha1.APIBinding</code> resources in consumer workspaces.</li><li>Aggregates <code>ContentConfiguration</code> and <code>MarketplaceEntry</code> data into the dynamic forwarding storage exposed by each virtual workspace.</li></ul><p>The <code>MarketplaceEntry.status</code> field is empty in v0.3.0; the Marketplace UI infers installation state from <code>APIBinding</code> resources in the consumer workspace.</p><h2 id="exposed-virtual-workspaces" tabindex="-1">Exposed virtual workspaces <a class="header-anchor" href="#exposed-virtual-workspaces" aria-label="Permalink to &quot;Exposed virtual workspaces&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Name</th><th>Base URL</th><th>Resource</th><th>Schema source</th></tr></thead><tbody><tr><td><code>contentconfigurations</code></td><td><code>/services/contentconfigurations/clusters/&lt;path&gt;/...</code></td><td><code>contentconfigurations.ui.platform-mesh.io/v1alpha1</code></td><td>Loaded at runtime from the <code>APIResourceSchema</code> named by <code>--resource-schema-name</code> in the workspace named by <code>--resource-schema-workspace</code>.</td></tr><tr><td><code>marketplace</code></td><td><code>/services/marketplace/clusters/&lt;path&gt;/...</code></td><td><code>marketplaceentries.marketplace.platform-mesh.io/v1alpha1</code></td><td>Loaded from a schema embedded in the binary at build time.</td></tr></tbody></table><p>Both endpoints accept the kcp wildcard form <code>clusters/*</code>, which delivers a multi-workspace view that the consumer can fan out from a single connection. The URL shape carries one cluster path per request and one resource per virtual workspace; there is no per-export segment between <code>&lt;vw-name&gt;</code> and <code>clusters/</code>.</p><h2 id="how-it-fits-into-platform-mesh" tabindex="-1">How it fits into Platform Mesh <a class="header-anchor" href="#how-it-fits-into-platform-mesh" aria-label="Permalink to &quot;How it fits into Platform Mesh&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Component</th><th>Role</th></tr></thead><tbody><tr><td><a href="./portal.html">Platform Mesh Portal</a></td><td>Reads the aggregated <code>ContentConfiguration</code> view through the <a href="./kubernetes-graphql-gateway.html">Kubernetes GraphQL gateway</a> to render navigation for the active workspace.</td></tr><tr><td><a href="./marketplace.html">Marketplace</a></td><td>Reads <code>MarketplaceEntry</code> through the gateway and writes <code>APIBinding</code> resources directly to the consumer workspace.</td></tr><tr><td><a href="./kubernetes-graphql-gateway.html">Kubernetes GraphQL gateway</a></td><td>Introspects each virtual workspace through a <code>gateway.platform-mesh.io/v1alpha1.ClusterAccess</code> resource and serves the result as GraphQL.</td></tr><tr><td><a href="https://github.com/platform-mesh/extension-manager-operator" target="_blank" rel="noreferrer">extension-manager-operator</a></td><td>Validates <code>ContentConfiguration.spec</code> and writes the processed result into <code>.status</code>. The <code>contentconfigurations</code> virtual workspace reads that processed state.</td></tr><tr><td><a href="./kcp.html">kcp</a></td><td>Upstream framework. Provides the virtual workspace primitives (<code>pkg/virtual/framework</code>), the <code>forwardingregistry</code> proxy storage, and the front-proxy that publishes the virtual workspace URLs.</td></tr></tbody></table>',18)),(t(),i(h,null,{default:a(()=>[l(r,{id:"mermaid-175",class:"mermaid",graph:"flowchart%20LR%0A%20%20portalUI%5B%22Portal%20UI%22%5D%20--%3E%20gateway%0A%20%20marketplaceUI%5B%22Marketplace%20UI%22%5D%20--%3E%20gateway%0A%0A%20%20gateway%5B%22Kubernetes%20GraphQL%20gateway%22%5D%0A%20%20gateway%20--%3E%7C%22GraphQL%20via%20ClusterAccess%22%7C%20vws%0A%0A%20%20vws%5B%22virtual-workspaces%22%5D%0A%20%20vws%20--%3E%7C%22read%20CCs%20and%20APIBindings%22%7C%20kcp%5B%22kcp%20shards%22%5D%0A%0A%20%20marketplaceUI%20-.%20%22create%2Fdelete%20APIBinding%22%20.-%3E%20kcp%0A"})]),fallback:a(()=>[...e[0]||(e[0]=[n(" Loading... ",-1)])]),_:1})),e[2]||(e[2]=o('<h2 id="authentication" tabindex="-1">Authentication <a class="header-anchor" href="#authentication" aria-label="Permalink to &quot;Authentication&quot;">​</a></h2><p>Authentication is layered. The standard delegating authentication options accept tokens through <code>--authentication-kubeconfig</code>, matching the kube-apiserver conventions. In addition, the binary registers a custom bearer-token authenticator that probes upstream kcp at <code>&lt;root&gt;/clusters/&lt;path&gt;/version</code> with the caller&#39;s token. Both are unioned, so a request is authenticated if either path accepts it.</p><p>The custom authenticator returns the user as <code>system:anonymous</code> in group <code>system:authenticated</code>. It treats kcp responses <code>200</code>, <code>201</code>, and <code>403</code> as success: a <code>403</code> from kcp means kcp recognized the token but denied the specific call, which the <code>virtual-workspaces</code> server interprets as proof that the token is valid, not that the call is allowed.</p><p>The custom authenticator does not propagate the upstream identity. Downstream code only sees the synthetic <code>system:anonymous</code> identity, so per-user authorization decisions inside <code>virtual-workspaces</code> are not yet possible.</p><h2 id="authorization" tabindex="-1">Authorization <a class="header-anchor" href="#authorization" aria-label="Permalink to &quot;Authorization&quot;">​</a></h2><p>Authorization is intentionally minimal in v0.3.0. The kcp <code>VirtualWorkspaceAuthorizer</code> runs at the framework root, and each named virtual workspace adds an <code>AttributesKeeper</code> wrapping a single decision: <code>system:authenticated</code> is allowed, every other principal is denied.</p><p>Any authenticated principal can read every <code>ContentConfiguration</code> and <code>MarketplaceEntry</code> reachable through the served virtual workspaces. The component is intended for use behind the kcp front-proxy and inside trusted Platform Mesh control planes; it does not enforce per-resource Platform Mesh authorization in v0.3.0. A <code>SubjectAccessReview</code>-based authorizer is tracked as future work.</p><h2 id="api-surface" tabindex="-1">API surface <a class="header-anchor" href="#api-surface" aria-label="Permalink to &quot;API surface&quot;">​</a></h2><h3 id="marketplaceentry" tabindex="-1"><code>MarketplaceEntry</code> <a class="header-anchor" href="#marketplaceentry" aria-label="Permalink to &quot;`MarketplaceEntry`&quot;">​</a></h3><p>The component owns the <code>MarketplaceEntry</code> cluster-scoped resource:</p><ul><li>API group: <code>marketplace.platform-mesh.io</code></li><li>Version: <code>v1alpha1</code></li><li>Scope: <code>Cluster</code></li><li>Storage in v0.3.0: served virtually from the <code>marketplace</code> virtual workspace; the embedded <code>APIResourceSchema</code> is exposed through the <code>marketplace.platform-mesh.io</code> <code>APIExport</code>.</li></ul><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>spec.apiBindingName</code></td><td>string</td><td><code>metadata.name</code> of the <code>APIBinding</code> backing this installation in the consumer workspace. Empty when the provider is not installed.</td></tr><tr><td><code>spec.providerMetadata</code></td><td>object</td><td>Provider metadata (name, descriptions, contacts, support, icons). Imported verbatim from the <a href="https://github.com/platform-mesh/extension-manager-operator" target="_blank" rel="noreferrer">extension-manager-operator</a> <code>ProviderMetadata</code> type.</td></tr><tr><td><code>spec.apiExport</code></td><td>object</td><td>The source <code>apis.kcp.io/v1alpha2.APIExport</code> whose schemas the entry exposes for binding.</td></tr><tr><td><code>status</code></td><td>object</td><td>Empty in v0.3.0.</td></tr></tbody></table><h3 id="marketplace-platform-mesh-io-apiexport" tabindex="-1"><code>marketplace.platform-mesh.io</code> APIExport <a class="header-anchor" href="#marketplace-platform-mesh-io-apiexport" aria-label="Permalink to &quot;`marketplace.platform-mesh.io` APIExport&quot;">​</a></h3><p>The component ships a static <code>apis.kcp.io/v1alpha2.APIExport</code> named <code>marketplace.platform-mesh.io</code>. The export references the <code>marketplaceentries</code> schema. Consumer workspaces bind provider <code>APIExports</code> to install provider APIs, while the Marketplace UI reads the catalog via the <code>marketplace</code> virtual workspace and infers installation state from <code>APIBindings</code> in the consumer workspace.</p><h2 id="url-contract" tabindex="-1">URL contract <a class="header-anchor" href="#url-contract" aria-label="Permalink to &quot;URL contract&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Pattern</th><th>Purpose</th></tr></thead><tbody><tr><td><code>/services/contentconfigurations/clusters/&lt;path&gt;/...</code></td><td>Per-workspace view of <code>ContentConfiguration</code> for the requested logical cluster.</td></tr><tr><td><code>/services/contentconfigurations/clusters/*/...</code></td><td>Wildcard view of <code>ContentConfiguration</code> across all clusters the caller can read.</td></tr><tr><td><code>/services/marketplace/clusters/&lt;path&gt;/...</code></td><td>Per-workspace view of <code>MarketplaceEntry</code> for the requested logical cluster.</td></tr><tr><td><code>/services/marketplace/clusters/*/...</code></td><td>Wildcard view of <code>MarketplaceEntry</code> across all clusters the caller can read.</td></tr></tbody></table><p>When a request matches one of these patterns, the server resolves the cluster name through kcp&#39;s logical-cluster machinery and forwards reads to the upstream resource using <code>forwardingregistry</code>. See <a href="./kcp/virtual-workspaces.html">kcp virtual workspaces</a> for the broader URL convention.</p><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><h3 id="component-flags" tabindex="-1">Component flags <a class="header-anchor" href="#component-flags" aria-label="Permalink to &quot;Component flags&quot;">​</a></h3><p>The <code>start</code> subcommand accepts the following component flags. Defaults reflect the values that ship in v0.3.0; chart values that override the binary defaults are noted.</p><table tabindex="0"><thead><tr><th>Flag</th><th>Effective default in v0.3.0</th><th>Description</th></tr></thead><tbody><tr><td><code>--kubeconfig</code></td><td>(required)</td><td>Kubeconfig used to reach the kcp root API server.</td></tr><tr><td><code>--server-url</code></td><td><code>https://frontproxy-front-proxy.platform-mesh-system:8443</code></td><td>Overrides <code>clusters[].cluster.server</code> from the kubeconfig. Must point at the kcp root URL, not the <code>root</code> cluster.</td></tr><tr><td><code>--entity-label</code></td><td><code>ui.platform-mesh.io/entity</code></td><td>Label key used by the <code>contentconfigurations</code> virtual workspace to identify provider-published <code>ContentConfiguration</code> resources.</td></tr><tr><td><code>--content-for-label</code></td><td><code>ui.platform-mesh.io/content-for</code></td><td>Label key indicating which workspace entity a <code>ContentConfiguration</code> applies to.</td></tr><tr><td><code>--main-entity-name</code></td><td><code>main</code></td><td>Entity name selected for the <code>root:orgs</code> workspace.</td></tr><tr><td><code>--account-entity-name</code></td><td><code>core_platform-mesh_io_account</code></td><td>Entity name selected for account workspaces.</td></tr><tr><td><code>--resource-schema-name</code></td><td><code>v250704-6d57f16.contentconfigurations.ui.platform-mesh.io</code></td><td>Name of the <code>apis.kcp.io/v1alpha1.APIResourceSchema</code> exposed by the <code>contentconfigurations</code> virtual workspace.</td></tr><tr><td><code>--resource-schema-workspace</code></td><td><code>root:platform-mesh-system</code></td><td>Workspace path that holds the <code>APIResourceSchema</code>.</td></tr><tr><td><code>--resource-apiexport-endpointslice-name</code></td><td><code>core.platform-mesh.io</code></td><td><code>APIExportEndpointSlice</code> name reserved for endpoint discovery. The flag is wired by the chart in v0.3.0 but is not consumed by the current request paths.</td></tr></tbody></table><p>The <code>--entity-label</code> and <code>--content-for-label</code> defaults match the labels documented for the <a href="/pr-preview/pr-197/reference/resources/content-configuration.html"><code>ContentConfiguration</code> resource</a> and the <a href="/pr-preview/pr-197/reference/resources/metadata-catalog.html">Metadata catalog</a>.</p><h3 id="standard-apiserver-flags" tabindex="-1">Standard apiserver flags <a class="header-anchor" href="#standard-apiserver-flags" aria-label="Permalink to &quot;Standard apiserver flags&quot;">​</a></h3><p>The component embeds the standard <code>k8s.io/apiserver</code> <code>SecureServingOptions</code> and <code>DelegatingAuthenticationOptions</code>. The chart sets the following:</p><table tabindex="0"><thead><tr><th>Flag</th><th>Default in chart</th><th>Description</th></tr></thead><tbody><tr><td><code>--bind-address</code></td><td><code>0.0.0.0</code></td><td>Apiserver bind address.</td></tr><tr><td><code>--secure-port</code></td><td><code>8443</code></td><td>HTTPS port.</td></tr><tr><td><code>--tls-cert-file</code></td><td><code>/certs/tls.crt</code></td><td>Serving certificate.</td></tr><tr><td><code>--tls-private-key-file</code></td><td><code>/certs/tls.key</code></td><td>Serving certificate key.</td></tr><tr><td><code>--client-ca-file</code></td><td><code>/client-ca/tls.crt</code></td><td>Front-proxy client CA.</td></tr><tr><td><code>--requestheader-client-ca-file</code></td><td><code>/requestheader-client-ca/tls.crt</code></td><td>Request-header client CA.</td></tr><tr><td><code>--requestheader-username-headers</code></td><td><code>X-Remote-User</code></td><td>Request-header username header.</td></tr><tr><td><code>--requestheader-group-headers</code></td><td><code>X-Remote-Group</code></td><td>Request-header group header.</td></tr><tr><td><code>--requestheader-extra-headers-prefix</code></td><td><code>X-Remote-Extra-</code></td><td>Request-header extra-attribute header prefix.</td></tr><tr><td><code>--authentication-kubeconfig</code></td><td><code>/api-authentication-kubeconfig/kubeconfig</code></td><td>Kubeconfig used by the standard delegating authenticator.</td></tr><tr><td><code>--authentication-skip-lookup</code></td><td>(set)</td><td>Skip the kube-apiserver authentication ConfigMap lookup.</td></tr></tbody></table><h2 id="deployment-and-platform-mesh-wiring" tabindex="-1">Deployment and Platform Mesh wiring <a class="header-anchor" href="#deployment-and-platform-mesh-wiring" aria-label="Permalink to &quot;Deployment and Platform Mesh wiring&quot;">​</a></h2><p>The <code>platform-mesh-operator-components</code> chart installs the component when <code>services.virtual-workspaces.enabled</code> is <code>true</code>. This is the default in a standard Platform Mesh installation.</p><table tabindex="0"><thead><tr><th>Value</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>services.virtual-workspaces.enabled</code></td><td><code>true</code></td><td>Includes <code>virtual-workspaces</code> in the standard Platform Mesh installation.</td></tr><tr><td><code>services.virtual-workspaces.values.deployment.serverUrl</code></td><td><code>https://frontproxy-front-proxy.platform-mesh-system:8443</code></td><td>kcp root URL passed to <code>--server-url</code>.</td></tr><tr><td><code>services.virtual-workspaces.values.deployment.resourceSchemaWorkspace</code></td><td><code>root:platform-mesh-system</code></td><td>Workspace holding the <code>APIResourceSchema</code> for <code>ContentConfiguration</code>.</td></tr><tr><td><code>services.virtual-workspaces.values.deployment.resourceSchemaName</code></td><td><code>v250704-6d57f16.contentconfigurations.ui.platform-mesh.io</code></td><td>Pinned <code>APIResourceSchema</code> name.</td></tr></tbody></table><p>The <code>virtual-workspaces</code> chart itself (<code>charts/virtual-workspaces</code>) wires:</p><table tabindex="0"><thead><tr><th>Value</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>image.name</code></td><td><code>ghcr.io/platform-mesh/virtual-workspaces</code></td><td>Container image.</td></tr><tr><td><code>service.port</code></td><td><code>8443</code></td><td>HTTPS service port.</td></tr><tr><td><code>kubeconfigSecretName</code></td><td><code>account-operator-kubeconfig</code></td><td>Secret mounted at <code>/api-kubeconfig</code>. The kubeconfig host is replaced by <code>--server-url</code>.</td></tr><tr><td><code>authenticationKubeconfigSecretName</code></td><td><code>portal-kubeconfig</code></td><td>Secret mounted at <code>/api-authentication-kubeconfig</code> for the delegating authenticator.</td></tr><tr><td><code>clientCASecretName</code></td><td><code>root-front-proxy-client-ca</code></td><td>Front-proxy client CA secret.</td></tr><tr><td><code>requestHeaderClientCASecretName</code></td><td><code>root-requestheader-client-ca</code></td><td>Request-header client CA secret.</td></tr><tr><td><code>cert.secretName</code></td><td><code>virtual-workspaces-cert</code></td><td>Serving certificate secret, populated by cert-manager.</td></tr><tr><td><code>cert.issuer.name</code></td><td><code>root-server-ca</code></td><td>Cert-manager <code>Issuer</code> used to mint the serving certificate.</td></tr><tr><td><code>clusterAccess.marketplace.enabled</code></td><td><code>true</code></td><td>Renders a <code>gateway.platform-mesh.io/v1alpha1.ClusterAccess</code> named <code>marketplace</code> so the <a href="./kubernetes-graphql-gateway.html">Kubernetes GraphQL gateway</a> can introspect the marketplace virtual workspace.</td></tr></tbody></table><p>The rendered <code>ClusterAccess</code> resource uses <code>introspectionPath: /clusters/*</code> and <code>requestPathTemplate: /clusters/{clusterTarget}</code> so the gateway routes per-workspace GraphQL requests to the matching cluster path on the virtual workspace.</p><h2 id="known-limitations-in-v0-3-0" tabindex="-1">Known limitations in v0.3.0 <a class="header-anchor" href="#known-limitations-in-v0-3-0" aria-label="Permalink to &quot;Known limitations in v0.3.0&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Topic</th><th>Status</th></tr></thead><tbody><tr><td>Authorization</td><td>Allows every <code>system:authenticated</code> principal. There is no per-resource check; treat the served data as visible to anyone with a valid kcp token.</td></tr><tr><td>Authenticator</td><td>Returns <code>system:anonymous</code>/<code>system:authenticated</code>. Identity is not propagated downstream, so per-user authorization is not yet possible.</td></tr><tr><td><code>MarketplaceEntry.status</code></td><td>Empty. The Marketplace UI infers installation state from <code>APIBinding</code> resources in the consumer workspace.</td></tr><tr><td>Readiness</td><td>The readiness probe accepts traffic before all upstream watches have synced; readiness is not gated on cache sync in v0.3.0.</td></tr><tr><td>Sharding</td><td>The component runs as a single deployment. Multi-shard kcp setups need additional front-proxy mapping; v0.3.0 ships against the single-shard local setup.</td></tr><tr><td>Schema pinning</td><td><code>--resource-schema-name</code> pins the <code>APIResourceSchema</code> for <code>ContentConfiguration</code>. Updating the schema requires a redeploy with a matching value.</td></tr><tr><td><code>--resource-apiexport-endpointslice-name</code></td><td>Defined and templated by the chart but not consumed by request paths in v0.4.7.</td></tr></tbody></table><h2 id="local-setup" tabindex="-1">Local setup <a class="header-anchor" href="#local-setup" aria-label="Permalink to &quot;Local setup&quot;">​</a></h2><p>The component ships with the standard Platform Mesh local setup. See <a href="/pr-preview/pr-197/how-to-guides/set-up-platform-mesh-locally.html">Set up Platform Mesh locally</a>.</p><h2 id="repository" tabindex="-1">Repository <a class="header-anchor" href="#repository" aria-label="Permalink to &quot;Repository&quot;">​</a></h2><ul><li><a href="https://github.com/platform-mesh/virtual-workspaces" target="_blank" rel="noreferrer">github.com/platform-mesh/virtual-workspaces</a></li><li><a href="https://github.com/platform-mesh/helm-charts/tree/main/charts/virtual-workspaces" target="_blank" rel="noreferrer">Helm chart</a></li></ul><h2 id="related" tabindex="-1">Related <a class="header-anchor" href="#related" aria-label="Permalink to &quot;Related&quot;">​</a></h2><ul><li><a href="./kcp/virtual-workspaces.html">kcp virtual workspaces</a> — kcp framework primitives and URL conventions</li><li><a href="./marketplace.html">Marketplace</a> — UI consumer of the <code>marketplace</code> virtual workspace</li><li><a href="./portal.html">Platform Mesh Portal</a> — UI consumer of the <code>contentconfigurations</code> virtual workspace</li><li><a href="./kubernetes-graphql-gateway.html">Kubernetes GraphQL gateway</a> — fronts both virtual workspaces as GraphQL endpoints</li></ul>',39))])}const y=d(p,[["render",u]]);export{w as __pageData,y as default};
